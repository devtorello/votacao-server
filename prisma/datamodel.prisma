type User {
  id: ID! @id
  firstName: String!
  lastName: String!
  CPF: String! @unique
  password: String!
  level: String! @default(value: "USER")
  votes: [Vote] @relation(link: INLINE)
}

type Candidate {
  id: ID! @id
  fullName: String!
  CPF: String! @unique
  Apartamento: String!
  URL: String!
  votes: [Vote] @relation(link: INLINE)
}

type Vote {
  id: ID! @id
  CPF: String! 
  userId: String! @unique
}
